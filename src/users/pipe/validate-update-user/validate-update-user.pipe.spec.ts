import { ValidateUpdateUserPipe } from './validate-update-user.pipe';

describe('ValidateUpdateUserPipe', () => {
  it('should be defined', () => {
    expect(new ValidateUpdateUserPipe()).toBeDefined();
  });
});
